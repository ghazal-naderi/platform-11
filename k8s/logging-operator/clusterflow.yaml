apiVersion: logging.banzaicloud.io/v1beta1
kind: ClusterFlow
metadata:
  name: loki-flow
  namespace: logging-operator
spec:
  match:
    - select: {}
  filters:
    - tag_normaliser: {}
    - parser:
        key_name: log
        parse:
          type: json
        remove_key_name_field: true
        reserve_time: true
        reserve_data: true
  outputRefs:
    - loki-output
