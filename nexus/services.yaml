---
apiVersion: v1
kind: Service
metadata:
  name: nexus
  labels:
    app: nexus-web
    group: service
spec:
  ports:
  - name: http-main
    port: 8081
    protocol: TCP
    targetPort: 8081
  selector:
    app: nexus
    group: service
  clusterIP: None

---
apiVersion: v1
kind: Service
metadata:
  name: nexus-0
spec:
  ports:
  - name: http-main
    port: 8081
    protocol: TCP
    targetPort: 8081
  selector:
    app: nexus
    group: service
    statefulset.kubernetes.io/pod-name: nexus-0